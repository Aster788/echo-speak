/** Shared centered column used by PageHeader hint and aligned controls (e.g. Review mode tiles). */
export const pageHintColumnClassName = "mx-auto w-fit max-w-full min-w-0";

export const pageHintColumnWidthVar = "--page-hint-column-width";
