/**
 * CLI: re-run expression extraction on existing transcripts.
 * Usage: npx tsx scripts/reprocess-expressions.ts [--transcript-id <id>]
 */

async function main() {
  console.log("reprocess-expressions: not yet implemented");
}

main().catch((err) => {
  console.error(err);
  process.exit(1);
});
