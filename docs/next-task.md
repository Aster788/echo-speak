# Next Task

**Create initial Supabase migration** — define `videos`, `transcripts`, `expressions`, and `reviews` tables per `docs/database.md`, enable RLS, and run `supabase db reset` locally.

After that: implement `transcript-cleaner` service and wire the import page.
